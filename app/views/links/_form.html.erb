<%= simple_form_for([@pot, @link]) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= f.input :name, input_html: {class: "span3"} %>
  
  <%= f.input :url, input_html: {class: "span3"} %>
  
  <%= f.input :description , input_html: {class: "span3"}%>
  
  <%= f.input :hottiness, collection: {"gut" => 1, "nicer Shit!" => 2, "Lesebefehl!" => 3}, input_html: {class: "span3"}, include_blank: false %>
  
  <%= hidden_field_tag :return_to, params[:url] if params[:url].present? %>

  <div class="control-group">
    <div class="controls">
      <% if params[:url].present? %>
        <%= f.submit "Link erstellen und zu #{params[:return_to].split('/')[2]} zurückkehren", class: "btn btn-large btn-primary" %>
      <% else %>
        <%= f.button :submit, "Link erstellen" %>
      <% end %>
    </div>
  </div>
<% end %>
